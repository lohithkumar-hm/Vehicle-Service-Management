@model IEnumerable<TestFrontend.Models.VehicleViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Vehicles List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VehicleNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OwnerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VehicleNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OwnerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @* @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@

                <a asp-action="Details" asp-controller="Vehicle"asp-route-num = "@item.VehicleNumber">Details</a> | 
                <a asp-action="Edit" asp-controller="Vehicle"asp-route-num = "@item.VehicleNumber">Edit</a> |
                <a asp-action="Delete" asp-controller="Vehicle"asp-route-num = "@item.VehicleNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
